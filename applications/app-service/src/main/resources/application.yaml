##Spring Configuration
server:
  port: 8080
spring:
  application:
    name: is-services-fidaback
  devtools:
    add-properties: false
  h2:
    console:
      enabled: true
      path: /h2
  profiles:
    include:
